import { render } from '@testing-library/react';
import Home from '../page';

describe('<Home/>', () => {
  it('only of not to happen error', () => {
    // render(<Home></Home>);
    expect(1).toBe(1);
  });
});
